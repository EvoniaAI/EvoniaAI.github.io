---
const socials = [
  { label: "Email", href: "mailto:annatar.he+evoniaai@gmail.com" },
  { label: "Founder", href: "https://i.annatarhe.com/" }
];
---

<footer class="bg-black p-4 w-full flex justify-center items-center flex-col">
  <ul
    class="max-w-screen-lg w-full text-xs text-teal-500 flex items-center justify-evenly"
  >
    {
      socials.map((item) => {
        const nameNode = <span class="p-4">{item.label}</span>;
        return (
          <li>
            <a href={item.href} target={item.href.startsWith('http') ? '_blank' : undefined} rel="noreferrer">
              {nameNode}
            </a>
          </li>
        );
      })
    }
  </ul>
  <p class="mt-4 text-center text-[0.65rem] uppercase tracking-[0.3em] text-white/30">
    © {new Date().getFullYear()} EvoniaAI · 灵栈平云
  </p>
</footer>